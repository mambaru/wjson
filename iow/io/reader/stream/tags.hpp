#pragma once

namespace iow{ namespace io{ namespace reader{ namespace stream{

struct _clear_;
struct _incoming_;
struct _initialize_;
struct _write_buffer_;
struct _read_buffer_;
struct _buffer_pool_;

}}}}

